# secrets — never commit the live config (holds the Odds API key)
config.json
# data caches & artifacts (regenerated by the pipeline)
data/*.json
data/*.bak
data/*.csv
data/ci/
out/
src/__pycache__/
__pycache__/
# private: strategy notes + paid-source provenance (kept out of public repo)
LESSONS.md
data/frozen_gw1/
ledger_decisions.jsonl
data/ledger_decisions.jsonl
# EXCEPTION: last-season baseline is public FPL data and IRREPLACEABLE after GW1
!data/baseline_prev.json
!data/static/
!data/season/
